# ── Core Telegram bot ─────────────────────────────────────────
python-telegram-bot==21.11.1

# ── AI / LLM clients ─────────────────────────────────────────
anthropic==0.51.0
openai==1.78.0        # used for Grok via xAI-compatible endpoint

# ── Market data ───────────────────────────────────────────────
requests==2.32.5
yfinance==1.2.0       # fallback only; Alpaca is primary on Railway

# ── Charting / chart image generation ────────────────────────
matplotlib==3.10.8
numpy==2.4.2

# ── Data wrangling ────────────────────────────────────────────
pandas==3.0.1

# ── Timezone ─────────────────────────────────────────────────
pytz==2026.1.post1
# v7.14.0 — zoneinfo backend for orb.live_runtime + engine.timing
# (DST-aware ET bucket conversions). python:3.11-slim ships without
# tzdata, so zoneinfo("America/New_York") raises without this.
tzdata>=2024.1

# ── HTML parsing (yfinance dep) ───────────────────────────────
lxml>=5.0
html5lib>=1.1

# ── Dashboard web UI (env-gated, v3.4.0+) ────────────────────
aiohttp>=3.9,<4.0

# ── Alpaca executor SDK (v4.0.0-alpha — TradeGeniusVal) ──────
alpaca-py==0.43.2

# -- Parquet bar/indicator cache (v6.9.0 backtest cache layer) -------
pyarrow>=15.0

# -- Replay harness wall-clock pin (v7.7.1, backtest-only) ----------
# Pins datetime.now / datetime.utcnow / time.time to the simulated
# clock during replay so backtest results are reproducible across
# wall-clock runs. Production code is unaffected.
freezegun>=1.5,<2.0
